import {View} from 'react-native';
import styled from 'styled-components';

export const HomeContainer = styled(View)`
  padding: 0px 0px;
  flex: 1;
`;

export const ListHeaderFooterComponent = styled(View)`
  height: 8px;
`;
